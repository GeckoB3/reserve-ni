/** User-facing message when booking APIs require an active service but none exist. */
export const AVAILABILITY_SETUP_REQUIRED_MESSAGE =
  'No active dining service is configured. Add at least one active service under Dashboard → Availability.';
