/**
 * Phase 3 product backlog (reference for prioritisation — not a commitment schedule).
 */
export const PHASE_3_BACKLOG_ITEMS = [
  'Any-available-practitioner appointment pooling (catalog + availability API)',
  'Guest-facing appointment reschedule flow',
  'Calendar blocks UI (API exists for practitioner blocks)',
  'Reporting enhancements and guest CRM export',
  'Playwright (or equivalent) E2E suite for Model A + B booking journeys',
] as const;
